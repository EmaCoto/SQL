SELECT * FROM users WHERE age=20; -- Culsulta toda la tabla de Users y con el where le especificamos que solo me traiga los ususarios que contengan en su campo age 20 
SELECT name FROM users WHERE age=20; --Trae los nombres de los ususarios que en su campo age tengan 20
SELECT DISTINCT name FROM users WHERE age=20; --Trae los nombres distintos de los ususarios que en su campo age tengan 20
